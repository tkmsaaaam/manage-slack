name: build
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'main.go'
      - 'daily.go'
      - 'go.mod'
      - 'go.sum'
      - '.github/workflows/build.yml'
permissions:
  actions: write
jobs:
  build-main:
    uses: ./.github/workflows/reusing_build.yml
    secrets:
      FILE_NAME: main
  build-daily:
    uses: ./.github/workflows/reusing_build.yml
    secrets:
      FILE_NAME: daily
